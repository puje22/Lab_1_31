// var n = 10;
// for (var i = 1; i < n; i++) {}
// console.log(i);

// const prompt = require("prompt-sync")();
// function fact(factorial) {
//   if (factorial <= 1) {
//     return 1;
//   } else {
//     return factorial * fact(factorial - 1);
//   }
// }
// // var n = prompt("Factorial oloh toog oruul: ");

// console.log(fact(n));

const prompt = require("prompt-sync");
var word = prompt("Text oruulna uu:  ");

for (var i = 0; i < word.length; i++) {
  var str = word[i];
  if (str == str.toUpperCase()) {
    convertedWord = word[i];
  } else {
  }
}
